/*
using System.Collections;
using System.Collections.Generic;
using UnityEngine;

public class StartPoint : MonoBehaviour
{
    private playermov thePlayer;
    //private CameraController theCamera;

    void Start()
    {
        thePlayer = FindObjectOfType<playermov>();
        thePlayer.transform.position = new Vector3(transform.position.x, transform.position.y, thePlayer.transform.position.z);

        //theCamera = FindObjectOfType<CameraController>();
        //theCamera.transform.position = new Vector3(transform.positionx, transform.position.y, theCamera.transform.position.z)
    }
}
*/
